# Use a dedicated PostgreSQL database. Never reuse the normal DATABASE_URL.
TEST_DATABASE_URL=

# Exact safety token required by test migrations and database integration tests.
TEST_DATABASE_GUARD=13log-test-only
