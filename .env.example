DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/DATABASE?sslmode=require"
DIRECT_URL="postgresql://USER:PASSWORD@HOST:5432/DATABASE?sslmode=require"
ADMIN_USERNAME="replace-with-admin-username"
ADMIN_PASSWORD=""
SESSION_SECRET=""
RISK_INTERNAL_SECRET=""
BLOB_READ_WRITE_TOKEN="replace-with-vercel-blob-read-write-token"
NEXT_PUBLIC_SITE_URL="https://example.com"
# Optional dedicated encryption secret for the cat's stored API key (>=32 chars).
# Falls back to SESSION_SECRET. Rotating it requires saving the API key again.
CAT_SETTINGS_SECRET=""
